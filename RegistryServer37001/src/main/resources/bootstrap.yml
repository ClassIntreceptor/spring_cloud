
eureka:
#  ip,在外部配置了配置文件，在此可省略
#  instance:
#    hostname: registryserver1
  client:
# 表明自己是服务端
    registerWithEureka: false
    fetchRegistry: false
# 单机版注册中心
#    serviceUrl:
#      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
# 集群版，本项目采用集群版，两个节点
    serviceUrl:
      defaultZone: http://${config-server.node1.instance-ip}:${config-server.node1.instance-port}/eureka/
spring:
  application:
    name: RegistryServer37001
#    表示引入外部文件
  profiles:
    include: doubleNode

